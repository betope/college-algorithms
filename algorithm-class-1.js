/*
Algorithm square of an number

integer: q, n

start
  write("Type a number: ")
  read n
  q ← n*n
  write("The squre is " + q)
end
*/

let q, n;
n =  prompt("Type a number");
q = n * n
console.log(`The square is ${q}`)

/*
Algorithm average value

real: media, a, b

start
  write("Type the first number: ")
  read a
  write("Type the second number: ")
  read b
  media ← (a + b) / 2
  write("The media value is " + media)
end
*/

let a, b, m
a = prompt("Type the first number")
b = prompt("Type the second number")

m = (a + b) / 2
